# V1.1-TIER2：第二档 2 项任务

**日期**：2026-04-12
**状态**：✅ 完成

## 任务清单

### 1. V1.1-向导整合（创建向导 5 步→4 步）

**改动**：
- 砍掉 Step 0 整屏模板选择网格，模板选择下沉为基本信息页面顶部的 `Select` 下拉框
- 步骤从 5 步精简为 4 步：基本信息 → 工具 → 环境变量 → 预览与创建
- `WizardData.template` 默认值从 `null` 改为 `"blank"`，始终有值
- 提取 `WizardTemplate` 类型别名，消除 `Exclude<..., null>` 类型体操
- `canNext` / footer 导航 / `handleCreateLocal` 全部适配新步骤索引
- i18n `steps` 数组更新 + 新增 `templateLabel` / `templateHint`

**涉及文件**：
- `src/components/skill-wizard/skill-wizard.tsx`
- `src/i18n/locales/{en,zh}.json`

### 2. V1.1-修改追踪（dirty → 区域级变更检测）

**改动**：
- `SkillEditState` 新增 `originalSnapshot: OriginalSnapshot`，编辑初始化时 `structuredClone` 原始数据
- 新增 `OriginalSnapshot` / `ChangeInfo` 类型和 `computeChanges()` 工具函数
- `getOrCreateEditState` 在创建 editState 时同步保存原始快照
- Navigator 树节点：amber 小圆点升级为数字 badge（1、2、3…），显示修改区域数量
- Inspector 面板："已修改" badge 升级为"N 处修改"，hover 显示具体区域（元数据、正文、文件名）
- 为后续 V1.1-Diff 任务铺路：`originalSnapshot` 可直接作为 diff 对比基准

**涉及文件**：
- `src/types/workspace.ts`（类型 + computeChanges）
- `src/hooks/use-workspace.ts`（snapshot 初始化）
- `src/components/workspace/navigator-panel.tsx`（数字 badge）
- `src/components/workspace/inspector-panel.tsx`（修改摘要）
- `src/i18n/locales/{en,zh}.json`

## 执行闸门

- [x] `npx tsc -b --noEmit` 无错误
- [x] `npm run build` 构建成功
- [x] 浏览器验证：向导 4 步流程 + 模板下拉 ✅
- [x] 浏览器验证：修改追踪 Navigator badge + Inspector 摘要 ✅

## 跳过调研说明

两项任务均为已有技术栈内的重复模式（React state + shadcn UI 组件），无需调研。
