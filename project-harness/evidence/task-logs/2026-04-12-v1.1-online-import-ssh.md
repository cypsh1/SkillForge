# Task Log — V1.1-在线导入 + V1.1-SSH

- **日期**: 2026-04-12
- **任务**: V1.1-在线导入（ClawHub / GitHub 导入）+ V1.1-SSH（远程服务器 Skill 读写）
- **状态**: 完成

## 决策记录

| 决策点 | 结论 |
|--------|------|
| SSH scope | 可配置路径，默认 `/root/.openclaw/workspace/skills/` |
| 离线缓存 | 缓存 + 简单冲突策略 |
| ClawHub 认证 | 不做 UI，代码预留 token 接口 |
| GitHub 认证 | V1.1 支持可选 token |

## 新增文件

| 文件 | 说明 |
|------|------|
| `src/lib/clawhub-api.ts` | ClawHub API 封装（search/meta/download） |
| `src/lib/github-api.ts` | GitHub API 封装（URL 解析 / Contents API / 递归下载） |
| `src/lib/skill-importer.ts` | 导入编排层（ClawHub + GitHub → 本地磁盘 → ParsedSkill） |
| `src/lib/remote-fs.ts` | 前端 SSH API 封装（6 个 invoke 调用） |
| `src/lib/app-config.ts` | 应用配置持久化（SSH 连接 + GitHub token） |
| `src-tauri/src/ssh.rs` | Rust SSH 模块（russh + SFTP + Tauri commands） |
| `src/components/import-dialog/import-dialog.tsx` | 导入对话框主体（Tabs） |
| `src/components/import-dialog/clawhub-tab.tsx` | ClawHub 搜索 + 预览 + 导入 |
| `src/components/import-dialog/github-tab.tsx` | GitHub URL → 目录预览 → 导入 |
| `src/components/ssh-panel/ssh-connect-dialog.tsx` | SSH 连接配置表单 |
| `src/components/ssh-panel/remote-skill-list.tsx` | 远程 Skill 浏览器 |

## 修改文件

| 文件 | 改动 |
|------|------|
| `src-tauri/Cargo.toml` | +tauri-plugin-http, +russh/russh-sftp/russh-keys, +tokio, +async-trait |
| `src-tauri/src/lib.rs` | 注册 http 插件 + ssh 模块 + 6 个 invoke handler |
| `src-tauri/capabilities/default.json` | HTTP URL 白名单 |
| `package.json` | +@tauri-apps/plugin-http |
| `src/types/skill.ts` | +SkillOrigin, +SkillBundle, ParsedSkill.origin |
| `src/lib/tauri-fs.ts` | +importSkillBundle() |
| `src/components/workspace/navigator-panel.tsx` | +Globe/Server/Cloud icons, +在线导入菜单项, +SSH 状态按钮, +3 个 dialog |
| `src/components/workspace/inspector-panel.tsx` | handleSaveAll SSH 回写逻辑 |
| `src/i18n/locales/zh.json` | +import.* (16 keys) + ssh.* (18 keys) + nav.importOnline |
| `src/i18n/locales/en.json` | 同上英文版 |

## 补充修复

- `capabilities/default.json`：写权限从 `skills/**` 扩展到 `$HOME/.openclaw/**`（支持 skillforge-config.json 写入）
- `ssh-connect-dialog.tsx`：接入 `app-config.ts`，打开 dialog 时加载上次连接配置，连接成功后自动保存
- `zh.json`：修复"在线导入"的 Unicode 破损字符

## 验证

- `npx tsc -b --noEmit` ✅
- `npm run build` ✅
- `cargo build` ✅
- 浏览器验证 ✅：Import Dialog（ClawHub + GitHub 双标签页）正常弹出，SSH Server 按钮可见

## 后续修复（同日第二轮）

1. `getFetch()` 增加 Tauri 环境检测，非 Tauri 降级为浏览器原生 fetch
2. ClawHub 域名从 `registry.clawhub.com` 改为 `clawhub.ai`（真实地址）
3. ClawHub API 路径修正：搜索 `/api/search`，详情 `/api/v1/skills/{slug}`，下载 `/api/v1/download?slug=`
4. ClawHub 类型修正：`displayName`/`summary`/`updatedAt`（去掉不存在的 `name`/`description`/`stars`/`author`）
5. ClawHub 搜索 UI 重做：单列列表 + 内联下载按钮
6. FS 写权限扩展到 `$HOME/.openclaw/**`
7. SSH 配置持久化接入 UI

## 端到端验证状态

- ClawHub 搜索 ✅（浏览器直接调通，返回真实结果）
- ClawHub 下载 ⏳（路径已确认正确，429 限流待解除）
- GitHub 导入 ⏳（未测真实 URL）
- SSH 全流程 ⏳（需要 tauri dev + 真实服务器）

## 跳过调研说明

ClawHub API 通过 curl 实际探测确认路径和响应格式。GitHub API 是标准 REST API。russh crate 通过 cargo doc 验证 API 兼容性。
