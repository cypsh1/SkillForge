# Task Log — V1.1-SSH自动重连 + V1.1-批量操作

**日期**：2026-04-12
**任务 ID**：V1.1-BATCH+SSH

## 改动

### 1. SSH 自动重连（~15 行）

`navigator-panel.tsx` — 新增 mount useEffect：
- 检查 `isTauri()` 环境
- 调用 `loadAppConfig()` 加载已保存的 SSH 配置
- 若有配置，调用 `connectSSH(saved)` 自动连接
- 失败静默（不弹 dialog，不报错），用户可手动连接

### 2. 批量验证 Dialog

`batch-dialog.tsx`（新建）— `BatchValidationDialog` 组件：
- 对全部已加载 skills 运行 `validateSkill()`（复用现有验证器）
- 表格展示：技能名称 | 状态图标（✅❌⚠️）| 错误数 | 警告数
- 底部统计："共 N 个技能，M 个有错误，K 个有警告"

### 3. 批量导出

同文件 — `BatchExportDialog` 组件：
- 列出全部 skills
- 点击"批量导出"→ 序列化为 JSON manifest 下载
- 使用 `skill.rawContent` 原始内容 + `configFiles` + `extraFiles`
- 复用 `downloadJson()` 工具函数

### 4. Navigator 集成

`navigator-panel.tsx` — 工具栏新增 Layers 图标按钮 + 下拉菜单：
- "批量验证" → 打开 BatchValidationDialog
- "批量导出" → 打开 BatchExportDialog

### 5. i18n

zh.json + en.json 各新增 13 个 `workspace.batch.*` key

## 涉及文件

| 文件 | 操作 |
|------|------|
| `src/components/workspace/batch-dialog.tsx` | 新建 ~190 行 |
| `src/components/workspace/navigator-panel.tsx` | 修改 +30 行 |
| `src/i18n/locales/zh.json` | 修改 +13 key |
| `src/i18n/locales/en.json` | 修改 +13 key |

## 验收

- [x] tsc ✅
- [x] build ✅
- [x] 批量验证 Dialog 显示 19 个技能验证结果表格
- [x] 批量导出 Dialog 显示技能列表 + 下载按钮
- [x] SSH 自动重连需 Tauri 环境验证（代码逻辑已确认正确）

## 跳过调研

已有技术栈内的重复模式（Dialog + 复用现有 validator/serializer/download），无需调研。
